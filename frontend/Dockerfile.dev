FROM node:20-alpine
WORKDIR /app
# Install deps at build time — Docker layer caches this until package.json changes
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "80"]
